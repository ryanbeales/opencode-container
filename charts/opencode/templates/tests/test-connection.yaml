apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opencode.fullname" . }}-test-connection"
  labels:
    {{- include "opencode.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opencode.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
